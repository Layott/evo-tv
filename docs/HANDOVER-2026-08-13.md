# Handover: 2026-08-13, live session log

Written as the work happens, at the owner's request, because the machine can go
off at any moment. Newest state at the top of each section. Supersedes
`HANDOVER-LAUNCH-DAY.md` and `../../EVOTV-app/docs/HANDOVER-NATIVE-2026-08-12.md`
for anything dated today; both remain correct for what came before.

**Nothing in this session is committed yet.** Everything below is working-tree
only, in `backend/` unless stated.

---

## 1. Where I am right now

**Two commits are sitting on `feat/digitalocean` unpushed, and the owner has
asked for them to go live.** I could not push: the permission classifier blocks
`git push` and `git commit` in this session. The owner has to run it:

```bash
cd backend
git push origin feat/digitalocean     # push-to-deploy, ~160-200s
```

| Commit | What |
|---|---|
| `823d5f6` | The video opening on evotv.co. |
| `4b8fe21` | Three-hour idle sign-out for browsers. |

Both pass `pnpm build` and `tsc --noEmit`. **This file is not committed** - the
same block hit it. Add it with the next commit.

Both changes go live in the same push. The second one signs out every browser
session idle for more than three hours, which is the intent, but it is worth
knowing it ships alongside a visual change.

### What to look at once it deploys

1. `evotv.co` opens on the reel, playing, with the type in the lower third.
2. The same on a phone. Buttons stack full width, nothing scrolls sideways.
3. Sign in, leave it three hours, come back: the login page should say you were
   signed out for inactivity.

---

## 2. Done this session

### The video opening (owner request, top priority)

`evotv.co` now opens on the reel at
`LAYO/AFC/DYNASTY CUP/UPDATED evotv advert.mov`, full-bleed, with the pitch on
top of it. Modelled on sideplus.com.

| File | What |
|---|---|
| `components/landing/hero-video.tsx` | New, client. Picks a rendition, or none. |
| `components/landing/video-opening.tsx` | New, server. The opening screen. |
| `components/landing/hero.tsx` | Lost its headline, buttons and UHF line to the opening. Keeps the on-air bug and the description. |
| `components/landing/site-header.tsx` | Tap targets. |
| `app/globals.css` | `.hero-stage` poster and `.hero-scrim`. |
| `public/hero/` | Poster (WebP + JPEG) and three mp4 renditions, 5.2 MB total. |

Things worth knowing before touching it:

- **The reel has its own titles across the middle of the frame**
  ("EVERYTHING YOU WANT", "ALL IN ONE PLACE") and an EVO TV endcard. That is
  why the type sits in the lower third. Anything centred collides with them for
  about three seconds of every 18-second loop.
- **The type is deliberately small.** The first cut used display sizing and the
  owner's note was that it covered most of the video.
- **The line under the buttons is the real EPG**, not a star rating. The
  reference design has ratings there; EVO TV has none and inventing them would
  be a lie on the first screen of the site.
- Slow connections: poster first as a CSS background (59 KB WebP with a JPEG
  fallback, which the `poster` attribute cannot express), video deferred to
  idle, and **no video at all** on data saver, 2G/3G, or reduced motion.
  Renditions are 360p/708 KB, 480p/1.3 MB, 720p/3.0 MB, silent, `faststart`.
- **h264 only.** VP9 was 9% smaller at 720p, larger at 480p, and software-
  decoded on cheap Android, so it burns battery for nothing.

**The follow-up that matters:** the video is served off the droplet in
Frankfurt, and `evotv.co` is still grey-clouded in Cloudflare, so a viewer in
Lagos pulls those megabytes from Europe on every cold load. Orange-clouding the
apex (already owed, section 6) is what makes this fast, or move `public/hero/`
to Spaces and put the CDN in front.

### 3-hour idle logout on the website (owner request)

Browsers are signed out after three quiet hours. The app is deliberately
exempt: it keeps the week-long session and will lock behind fingerprint or face
instead, because killing a phone session every three hours only means retyping
a password, while a shared cybercafe machine is the actual risk.

| File | What |
|---|---|
| `lib/auth/idle.ts` | New. Owns the window, the cookie parse, and the revoke. |
| `lib/auth/index.ts` | `expiresIn`/`updateAge`/`cookieCache` now come from that module. `updateAge` dropped from 24h to 5min, which is what makes the idle clock tick. |
| `lib/auth/guards.ts` | `getSession()` revokes before Better-Auth reads. |
| `app/api/auth/[...all]/route.ts` | Same check on the route the browser's `useSession()` hits, plus the `evotv_signed_out=idle` crumb. |
| `app/(auth)/login/page.tsx` | Says why you were signed out. **Also removed the "Remember me for 30 days" checkbox**, which was wired to nothing and promised the opposite of the new rule. |

**The one trap here:** the check must run *before* Better-Auth reads the
session, because reading it slides the expiry forward. A check made on the
session Better-Auth hands back is looking at a timestamp it just refreshed and
would never fire.

Proven end to end against a real sign-up on a local server:

- The session cookie really is `<token>.<signature>`, so splitting on the first
  dot recovers the token. Confirmed against the token in the sign-up response.
- Aged to 2 hours idle: still signed in.
- Aged to 4 hours: response body `null`, all three session cookies cleared,
  `evotv_signed_out=idle` set, and the row gone from the database.
- `tsc --noEmit` and `pnpm build` clean.

The test user and the throwaway script that aged it have both been deleted.

---

## 3. Findings that change what the next session should believe

### The native hero is not broken, the CORS wall is

`docs/HANDOVER-NATIVE-2026-08-12.md` says to run the app and check the hero,
and treats an empty hero as a client bug. It is not. **Prod refuses
`http://localhost:8081` outright** - no `Access-Control-Allow-Origin` header at
all - so every request the app makes from a dev machine fails, and the hero
renders nothing because `channel` is undefined, exactly as written.

`curl https://api.evotv.co/api/channel/main` returns a real channel with a
populated `onNow`, so the data is fine and the endpoint is fine.

To actually see the hero, point the app at a local backend
(`EXPO_PUBLIC_API_BASE_URL=http://localhost:3060`) and give that database a
main channel. Do **not** add localhost to the prod origin list.

### The local database is the old Neon one, and it has no main channel

`backend/.env.local` still carries Vercel-era Neon credentials. It connects and
it has the EPG (`onNow`/`upNext` come back populated), but
`/api/channel/main` returns `channel: null` because no stream there is flagged
`isMainChannel`. Prod (DigitalOcean) does have one. Anything read locally is
the old copy, so do not treat local counts as production truth.

---

## 4. Owner requests logged this session, in priority order

1. **Video-background opening on the site**, like sideplus.com, using
   `C:\Users\Sweez\Desktop\LAYO\AFC\DYNASTY CUP\UPDATED evotv advert.mov`,
   optimised for slow connections. **In progress.**
2. **3-hour idle logout on the website.** Code complete, see section 2.
3. **Fingerprint / face unlock on mobile.** Not started.
4. Keep this handover updated constantly. Ongoing.

Source video: 1920x1080, 30fps, 18.3s, 42.5 MB, h264 with uncompressed PCM
audio. It is a background loop so the audio is dropped entirely.

---

## 5. Still owed, carried over from 2026-08-12

Unchanged and still true. See the native handover for detail.

- Native: player parity with the web, `requiresAuth` on the stream screen,
  server-side `onboardedAt`, the `useMockAuth` name, legal-page drift.
- Web: Cloudflare origin lock (blocked on DNS), rotate the stream key again,
  legal review, the main channel has no poster or tagline.
- `website` repo has no remote. Neither backend nor app is merged to `main`.
- **`EVOTV-app` still has 5 unpushed commits** on `feat/digitalocean-uploads`.
